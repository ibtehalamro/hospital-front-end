export const messages = {
    "required": " is required."
    , "minLength": " must be at least 2 character's long."
}

export const registerObject = {
    required: true
}
export const registerObjectNames = {
    required: true,
    minLength: 2
};
